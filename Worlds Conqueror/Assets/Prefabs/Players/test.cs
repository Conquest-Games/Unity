using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class test : MonoBehaviour
{
    Player player1 = new Player(1, 2, 4);
    

    // Start is called before the first frame update
    void Start()
    {
        //player1.RemoveOr(10);
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
